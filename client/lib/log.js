/* eslint no-console: "off" */

export default Object.freeze({
  debug(...messages) {
    console.log(...messages);
  },

  info(...messages) {
    console.log(...messages);
  },

  error(...messages) {
    console.log(...messages);
  }
});
